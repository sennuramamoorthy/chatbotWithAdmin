"""Reusable in-memory test doubles for the application ports.

Shipped in-package (like ``django.test``) so both the unit/integration suites and
future API-layer tests share the same deterministic fakes.
"""
